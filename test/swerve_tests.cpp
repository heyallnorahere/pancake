#include <gtest/gtest.h>

TEST(pancake, simple) {
    // todo: link library and send swerve request
}

int main(int argc, char** argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}