services:
  robot:
    image: ghcr.io/heyallnorahere/pancake:latest
    build: .
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config/:/pancake/config/
    devices:
      - /dev/input/:/dev/input/